name: echo
description: Echo back request details (via httpbin). Useful for testing agent HTTP plumbing.
upstream:
  base_url: https://httpbin.org
  path: /anything
  method: GET
  passthrough_params: []  # empty = pass everything through
pricing:
  price_usdc: "0.0001"
  network: base
  asset: USDC
  pay_to: 0xREPLACE_ME_BEFORE_DEPLOY
limits:
  max_per_minute: 60
